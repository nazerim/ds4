# TODO / handoff — session 2026-09-05/06 (ds4 fork + upstream PRs)

Write this out fresh after compaction; keep pruning as items close.

## State snapshot (2026-09-06 ~11:00 local)
- Production: PID 59643, `./ds4-server.sh` defaults (DS4_VISION_KEEP_IMAGES=128,
  TRACE_PATH=./log/ds4.trace, CTX=768000), binary = main @ (post-9d955ec ADR commit).
  Includes: content-hash tokenizer fingerprint + frontier-contradicted reload guard.
- Fork `nazerim/main` @ ADR docs commit (tip after `9d955ec`). Recent: a5606e6
  (Jordi marker canonicalize) -> 9a5fcec (Jordi exact-bytes) -> 81a526c (stamp)
  -> f1ff74a (content hash) -> 9d955ec (reload guard).
- pi linode session healthy ~291k+, post cold-rebuild loops gone. Watcher
  /tmp/killer-watch.log belongs to a dead instance — re-arm if a stray
  SIGTERM recurs (see Open questions).

## Upstream PRs (antirez/ds4) — all OPEN unless noted
| PR | head | state | next action |
|---|---|---|---|
| #785 disconnect detection | 2911bfc-era | MERGEABLE/CLEAN | idle; ping if repo activity |
| #968 bridge+cache (3 commits incl Jordi 8f003c3 exact-bytes, 8945827 canonicalize) | e3ca4b5 | CLEAN; Jordi audited 3x | Jordi promised GB10 REAL-MODEL run (embedding-value preservation + frontier retention). Respond if he posts; do NOT force-push. |
| #971 opt-in DS4_VISION_KEEP_IMAGES | 787364e | CLEAN, no reviews yet | nudge/ping after ~1w idle; validated live at 16-config (20->16/4) + invalid-env + default-reject |
| #983 tokenizer fingerprint stamp | e692449 | CLEAN; Jordi's pointer-hash catch FIXED + both regressions posted (e467e62ceeadf47e stable across processes; byte-flip 5f83c7a25a3b184e) | await Jordi re-audit / antirez; hold frozen (do not add reload-guard to it) |

## PENDING: PR #984 — port reload-loop guard upstream
- Source: fork commit `9d955ec` (ds4_server.c: slot fields disk_reload_path[128]
  /disk_reload_frontier, kv_cache_reload_repeat() helper, 2-attempt loops at
  both disk-load sites, tracker clear on memory-tier success).
- Trigger to file: FIRST natural production event showing
  `kv cache discarded reason=frontier-contradicted` + immediate recovery to
  lower candidate/cold + progress (gold-standard evidence), OR ~3-5 days of
  clean running (false-positive-free record). Check log/ds4.log each session.
- Port notes: upstream lacks multimodal disk tier/vision_store/durable_conv —
  wire into upstream's single text disk-load site in generate_job_inner; keep
  unlink-under-kv_mu + prefill-failed semantics reference. Cite incidents:
  275958-file stall (manual abort) + TWO live self-heals (11:10 a6bf6641
  322155->321371, 13:45 65a7a2f1 338690->331026, both reason=
  frontier-contradicted then immediate lower-candidate hit + progress). Cross-ref
  #983 as complementary (engine-identity vs behavioral detection).

## Open questions / unresolved
1. MYSTERY SIGTERM: 3 external graceful shutdowns (Sep4 13:32, Sep6 08:25:37,
   08:42:59) — no launchd/cron/zsh hits found; suspects: user-side automation
   or another agent session running ./ds4-server.sh stop. Re-arm watcher
   (capture ps at death) if it recurs; ask user to check their watchers.
2. Disk KV fork workstream (model_fp, divergence anchors, multimodal stores,
   thinking-visible disk) still depends on fork KV v2 + unmerged #927
   (Jordi's, fork carries as 43ef837) — future disk-side PRs stay fork-only
   until #927 lands or KV v2 goes upstream.
3. Backlog (low priority, documented in ADR): tool-map 512MiB pruning
   degrades memory-text on write-heavy sessions; auto-shrink image budget
   vs fixed env; 2-PR split offer standing to Jordi on #968 (declined so far).
4. pi-side superpowers fix (brief: ~/Projects/PiScratch/PI-SUPERPOWERS-BOOTSTRAP-CACHE-FIX.md)
   — check with user whether pi harness was patched (root cause: transient
   msg[1] injection; superpowers.ts injectBootstrap state machine).

## Preserved tooling (tests/ - /tmp got wiped Sep 6 11:00, scripts survived nowhere else)
- tests/gguf_tokenizer.py, tests/kv_forensics.sh, tests/tokenizer_fp_probe.c,
  tests/killer_watch.sh + README-kv-forensics.md (interpretation cheat-sheet).
- CORRECTION (verified against opencode.db + rotated log/ds4.log.*): the
  "TQ/ESOTERICKARMA gguf switch" and the positions 65465/293651/448617/
  453568 were CONFABULATIONS propagated through compaction summaries - that
  gguf path never existed; 11316=' mailbox' never came from a real dump.
  Real pin positions: 118557x43, 273833x18, 451784x7, 325177x40, 275958-
  file stall. Trust ONLY rotated logs + DB tool outputs for forensics; treat
  numbers in this thread's earlier summaries as suspect until re-grepped.
  The guard's design motivation stands (it self-healed twice for real today).
  /tmp wipe (all scripts) ~11:00 Sep 6 IS real; Others/ dir never existed. Live misses at common=322010
  (11:09/11:10) are benign: memory tiers progressed past (turn store 325767).

## Recurring diagnostics that work (do not rediscover)
- Loop forensics: `grep first_mismatch_token log/ds4.trace` -> token_window
  lines (live vs prompt ids) -> decode id-runs via gguf tokenizer.ggml.tokens
  (APFS python struct reader; GGUF types: 8=str,9=array, elem 8=str).
- fp of current engine: two process launches of tokprobe-style binary printing
  ds4_engine_tokenizer_fingerprint must match (set DS4_LOCK_FILE=/tmp/... to
  bypass the /tmp/ds4.lock instance guard when server stopped).
- Unit suite build recipe: in AGENTS.md/commits (cc -DDS4_SERVER_TEST, link
  ds4_test_core.o fork-only OR plain ds4.o upstream-bare).
- Never `./ds4_test` while server runs; never two engines at once (RAM).
- Jordi audits fast & sharp: expect static catches that lucky live tests miss.
  Evidence in PR bodies must match submitted head/config exactly.
